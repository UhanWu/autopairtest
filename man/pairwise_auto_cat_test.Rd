% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pairwise_auto_cat_test.R
\name{pairwise_auto_cat_test}
\alias{pairwise_auto_cat_test}
\title{Automatic Pairwise Test for Categorical Outcomes}
\usage{
pairwise_auto_cat_test(data, outcome, group)
}
\arguments{
\item{data}{A data frame.}

\item{outcome}{<\code{\link[dplyr:dplyr_data_masking]{data-masking}}> Unquoted name of
the categorical outcome column.}

\item{group}{<\code{\link[dplyr:dplyr_data_masking]{data-masking}}> Unquoted name of
the grouping column.}
}
\value{
A tibble with one row per group pair, containing:
\describe{
\item{group_1, group_2}{Group labels for the pair.}
\item{n_1, n_2}{Sample sizes per group.}
\item{test_used}{Either \code{"Chi-square Test"} or \code{"Fisher's Exact Test"}.}
\item{p_value}{P-value from the selected test.}
}
}
\description{
Performs pairwise group comparisons on a categorical outcome, automatically
selecting chi-square test or Fisher's exact test based on whether any
expected cell count falls below 5.
}
\details{
For each pair, a contingency table is built and chi-square is first run to
inspect expected counts. If any expected count is below 5, Fisher's exact
test is used instead. Rows with \code{NA} in either \code{outcome} or \code{group} are
dropped before analysis.
}
\examples{
set.seed(42)
df <- data.frame(
  result = sample(c("Yes", "No"), 90, replace = TRUE),
  grp    = rep(c("A", "B", "C"), each = 30)
)
pairwise_auto_cat_test(df, result, grp)

}
